# Claude Code (cấu hình công cụ/cục bộ, không cần đưa lên repo)
.claude/

# Windows
Thumbs.db
Desktop.ini
$RECYCLE.BIN/

# Mac
.DS_Store
.AppleDouble
.LSOverride

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# Logs
*.log
npm-debug.log*

# Temporary files
*.tmp
*.temp
~$*

# Node modules (nếu có)
node_modules/
.tools/

# Environment variables
.env
.env.local
.env.*
!.env.example

# Backup files
*.bak
*.backup
*.before_fix.js

# Tài liệu đáp án / lý thuyết - KHÔNG public lên web
*.docx

# Test files (nếu không muốn push)
# test/
# *.test.html

# ⚠️ BẢO MẬT: KHÔNG đẩy token GitHub lên repo công khai
# (Token được lưu trong localStorage trình duyệt, không nằm trong file.
#  Các dòng dưới chỉ để chặn lỡ tay tạo file chứa token.)
*token*
*TOKEN*
github-token.txt
secret*.js
